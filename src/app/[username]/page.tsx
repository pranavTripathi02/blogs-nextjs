import { api } from "~/trpc/server";

function UserProfile({ params }: { params: { username: string } }) {
  // const { username:userName } = params;
  //
  // let user;
  // try {
  //   user = await} catch (err) {
  //   console.error(err);
  //   return <NotFound />;
  // }
  return <div></div>;
}
export default UserProfile;
